-- Gruvbox colorscheme. Loaded but not activated by default — the active
-- colorscheme is nord (see nord.lua). Kept here for quick swap-in if needed.
return {
  "ellisonleao/gruvbox.nvim",
  priority = 1000,
  config = true,
  opts = {
    transparent_mode = true
  }
}
